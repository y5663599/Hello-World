"""
什么是数据容器
- 一种可以容纳多份数据的数据类型，容纳的每一份数据称之为1个元素
- 每一个元素，可以是任意类型的数据，如字符串，数字，布尔等

数据容器根据特点的不同，如：
- 是否支持重复元素
- 是否可以修改
- 是否有序，等
分为5类，分别是：
列表(list),元组(tuple),字符串(str),集合(set),字典(dict)
- 各有特点，但都满足可容纳多个元素的特性
"""
namePack = ["大雷","手雷","小雷"]
print(namePack[0])